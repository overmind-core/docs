---
title: Introduction
---

Overmind improves production AI agents with their own data. It ingests OpenTelemetry traces, scores them as they arrive, turns them into curated datasets, and feeds three kinds of work: eval experiments, optimisation runs that open pull requests, and training runs that produce models benchmarked against the one you ship today.

```flow
flow row
title: From your agent's process to a model you own
A[Your agent | SDK or any OTel exporter] -> B[OTLP ingest | POST /api/v1/traces]
B -> C[Spans in Postgres | scored on arrival]
C -> D[Datasets | curated from traces]
D -> E[Eval runs | variants vs baseline]
E -> OP[Optimiser | code diffs, replayed + scored]
D -> T[Training | trained models vs baseline]
T -> I[Inference | OpenAI-compatible API]
OP ~> A : winning diff lands as a PR
I ~> B : served model's calls are traced
```

Everything speaks standard protocols: OTLP/HTTP for ingest, REST JSON for the API, OpenAI-compatible serving for models.

| Surface | URL |
| --- | --- |
| API | `https://api.overmindlab.ai` |
| Console | `https://console.overmindlab.ai` |

Authenticate with a project-scoped `ovr_` key as `X-Api-Key: <key>` or `Authorization: Bearer <key>`. There is no proprietary agent framework — the SDKs auto-instrument OpenAI, Anthropic, and Gemini over OpenTelemetry, and any OTel exporter can POST to the same endpoint.

Overmind gives you:

- **Live scoring** — evaluators run on root spans as they land
- **Datasets from traffic** — convert traces into eval and training data
- **Eval runs** — variants measured against a baseline with typed scores
- **Optimiser** — local execution, scored candidates, a PR with the winning diff
- **Training** — managed GPU runs, loss curves, benchmark vs your production model
- **Inference** — OpenAI-compatible API for your trained models and frontier models

## Guides

<CardGroup cols={2}>
  <Card title="Quickstart" href="/quickstart" icon="rocket">
    API key, agent setup prompt, first trace in the console.
  </Card>
  <Card title="Observability" href="/core/observability" icon="eye">
    OTLP ingest, span model, attribute mapping, explorer.
  </Card>
  <Card title="Python SDK" href="/tracing/sdk-python" icon="python">
    `init()`, auto-instrumentation, decorators, context.
  </Card>
  <Card title="TypeScript SDK" href="/tracing/sdk-typescript" icon="node-js">
    `OvermindClient`, `initTracing()`, manual spans.
  </Card>
  <Card title="Agents" href="/core/agents" icon="robot">
    Repo scans, telemetry attribution, eval specs.
  </Card>
  <Card title="Datasets" href="/core/datasets" icon="database">
    Schema, intents, Workshop, trace-to-dataset.
  </Card>
  <Card title="Eval" href="/agent-testing/eval" icon="clipboard-check">
    Evaluator kinds, eval sets, run reports.
  </Card>
  <Card title="Optimisers" href="/agent-testing/optimisers" icon="wand-magic-sparkles">
    Loop, local executioner, resulting PR.
  </Card>
</CardGroup>

## Models & API

<CardGroup cols={2}>
  <Card title="Training" href="/models/training" icon="dumbbell">
    Runs, validation, loss curves, baseline benchmark.
  </Card>
  <Card title="Inference" href="/models/inference" icon="server">
    Serving lifecycle and `/api/v1/chat/completions`.
  </Card>
  <Card title="REST API" href="/platform/api" icon="code">
    Auth, endpoint map, conventions, Swagger.
  </Card>
  <Card title="Model Library" href="https://www.overmindlab.ai/models" icon="layer-group">
    Base models, context windows, training and inference pricing.
  </Card>
</CardGroup>
