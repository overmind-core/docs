---
title: Introduction
---

Overmind makes the AI in your product improve on its own production traffic. Point your agent's OpenTelemetry traces at it and every run is scored as it arrives; that traffic becomes curated datasets, and the datasets feed three loops: eval runs that measure a change, optimisation runs that rewrite your agent's code in your repository and prove the rewrite is better, and training runs that give you a model of your own, benchmarked against the one you ship today.

Overmind is open source. Use it hosted at overmindlab.ai, or [self-host](/platform/self-hosting) it with Docker Compose — the same code, the same workflows either way.

```flow
flow row
title: From your agent's process to a model you own
A[Your agent | SDK or any OTel exporter] -> B[OTLP ingest | POST /api/v1/traces]
B -> C[Stored spans | scored per task execution]
C -> D[Datasets | curated from traces]
D -> E[Eval runs | variants vs baseline]
E -> OP[Optimiser | code diffs, replayed + scored]
D -> T[Training | trained models vs baseline]
T -> I[Inference | OpenAI-compatible API]
OP ~> A : winning diff applied locally
I ~> B : served model's calls are traced
```

Everything speaks standard protocols: OTLP/HTTP for ingest, REST JSON for the API, [MCP](/platform/mcp) for coding agents, OpenAI-compatible serving for models.

| Surface | Hosted | Self-hosted default |
| --- | --- | --- |
| API | `https://api.overmindlab.ai` | `http://localhost:8000` |
| Console | `https://console.overmindlab.ai` | `http://localhost:5173` |
| MCP | `https://api.overmindlab.ai/api/mcp/` | `http://localhost:8000/api/mcp/` |

Authenticate with an `ovr_` key as `X-Api-Key: <key>` or `Authorization: Bearer <key>`. There is no proprietary agent framework — the Python SDK auto-instruments OpenAI, Anthropic, Gemini, Agno, and LangChain over OpenTelemetry, and any OTel exporter can POST to the same endpoint.

Overmind gives you:

- **Live scoring** — every trace is carved into task executions and judged against the capability's eval set
- **Datasets from traffic** — convert traces into eval and training data, shaped by the dataset's own agent
- **Eval runs** — variants measured against a baseline with typed scores
- **Optimiser** — candidates generated and replayed in your repository by the CLI, scored on the platform, the winning diff on the candidate for you to apply
- **Training** — managed GPU runs, loss curves, a benchmark against your production model
- **Inference** — OpenAI-compatible API for your trained models and frontier models
- **MCP and CLI** — the same workflows from Cursor, Claude Code, OpenCode, or Codex

## Guides

<CardGroup cols={2}>
  <Card title="Quickstart" href="/quickstart" icon="rocket">
    One pasted prompt: SDK, MCP, capability sync, first trace.
  </Card>
  <Card title="Self-hosting" href="/platform/self-hosting" icon="server">
    Docker Compose, the environment reference, backends and billing modes.
  </Card>
  <Card title="Observability" href="/core/observability" icon="eye">
    OTLP ingest, span model, attribute mapping, the executions view.
  </Card>
  <Card title="Python SDK" href="/tracing/sdk-python" icon="python">
    `init()`, the run boundary, decorators, tasks, runtime expectations.
  </Card>
  <Card title="Agent & Capabilities" href="/core/capabilities" icon="robot">
    Local discovery, sync, tasks, telemetry attribution.
  </Card>
  <Card title="Datasets" href="/core/datasets" icon="database">
    Source, cells, versions, contracts, the workshop agent.
  </Card>
  <Card title="Eval" href="/agent-testing/eval" icon="clipboard-check">
    Evaluator kinds, claims, task bindings, eval runs.
  </Card>
  <Card title="Trace scoring" href="/agent-testing/trace-scoring" icon="gauge-high">
    Units, tasks, verdicts, execution and session scores.
  </Card>
  <Card title="Optimisers" href="/agent-testing/optimisers" icon="wand-magic-sparkles">
    The client-driven loop, harness and backtesting modes, the winning diff.
  </Card>
  <Card title="MCP" href="/platform/mcp" icon="plug">
    Setup per coding agent, the tool catalog, prompts, resources.
  </Card>
</CardGroup>

## Models & API

<CardGroup cols={2}>
  <Card title="Training" href="/models/training" icon="dumbbell">
    Runs, the model catalog, loss curves, the baseline benchmark.
  </Card>
  <Card title="Inference" href="/models/inference" icon="microchip">
    Serving lifecycle, the capability alias, `/api/v1/chat/completions`.
  </Card>
  <Card title="REST API" href="/platform/api" icon="code">
    Auth, endpoint map, conventions, Swagger.
  </Card>
  <Card title="CLI" href="/platform/cli" icon="terminal">
    `overmind init`, `sync`, `dataset`, `optimise`, `model`.
  </Card>
</CardGroup>
