---
title: Glossary
description: Definitions of terms used across Overmind, as they appear in the console and the API.
---

## Platform structure

**Project** — the tenant boundary. Agents, traces, datasets, runs, models, connectors, and API keys belong to exactly one project. See [Administration](/platform/administration#projects).

**Agent** — your product: one per project, mapped as a graph of capabilities. It has no record of its own; its identity is the project. See [Agent & Capabilities](/core/capabilities).

**Capability** — one purpose the agent serves, and Overmind's record of it: code structure from a repository scan joined with runtime behaviour from telemetry. A capability the scan no longer finds leaves the agent and returns when the code does; a hand delete hides it and keeps its data. See [Agent & Capabilities](/core/capabilities).

**API key** — a project-scoped credential (`ovr_...`, stored hashed, shown once at creation) that authenticates the SDKs, OTLP ingest, the REST API, the CLI, and inference. Sent as `X-Api-Key` or `Authorization: Bearer`.

**Job** — the umbrella record for long-running work: eval runs, optimisation runs, training runs, and dataset analyses all surface in the Jobs view.

**Brain** — the console home: agentic chat over your project's agents, traces, and failures.

## Observability

**Trace** — one end-to-end run of an agent: the set of spans sharing a `trace_id`. There is no separate trace record; the root span (the one with no parent) represents the trace in lists.

**Span** — one operation inside a trace, stored with its full OTLP payload (timing, status, attributes, events) plus platform fields computed at ingest. Spans are typed: `entry_point`, `workflow`, `tool_call`, `function`, `retrieval`, or `llm_call`.

**Session** — traces grouped by a shared `conversation.id` attribute, representing one multi-turn exchange. Set via `set_conversation_id()` in either SDK.

**OTLP** — the OpenTelemetry wire protocol. Overmind ingests OTLP/HTTP **protobuf** at `POST /api/v1/traces`; JSON and gRPC are not accepted. See [Observability](/core/observability#the-otlp-endpoint).

**Connector** — a pull-based import from another tracing platform. Langfuse is available; LangSmith, Braintrust, Opik, Helicone, and Arize Phoenix are coming soon.

**Trace coverage** — the fraction of a capability's discovered components (entrypoints, tools) that emit telemetry, reported on its page.

**Observability PR** — a pull request Overmind opens against your repository adding SDK initialisation and span instrumentation.

## Data

**Dataset** — a curated collection of datapoints with a **source** (`custom`, `traces`, `external`), an **intent** (`eval` or `ft`), and for trace-built data a **surface** (`capability` or `model`). See [Datasets](/core/datasets).

**Datapoint** — one example: `input`, `expected_output`, optional tags/persona, an `extra` bag for non-canonical columns, and a `source_trace_id` when built from a trace.

**Data Workshop** — the dataset console: quality analysis (score out of 100 with a verdict), insights with staged fixes, commit history with restore, and export.

**Insight** — one analysis finding with severity, evidence, and usually a proposed, dry-run-validated fix.

**Commit** — one recorded change to a dataset. Eval runs pin the exact commit they scored, making results reproducible.

## Evaluation

**Evaluator** — one versioned scorer. Kinds: `llm_judge`, `deterministic`, `trajectory`, `statistical`, `agentic`, `dag`. See [Eval](/agent-testing/eval#evaluators).

**Rubric / checklist** — an LLM judge's plain-language criteria, compiled into weighted atomic checklist items; items can be hard **gates**.

**Eval set** — a named group of a capability's evaluators. Members have a role: **generative** (score eval-run outputs) or **trace scoring** (score live traces, with a sampling rate). One set per capability is **active**.

**Eval run** — a controlled experiment: one data source × N variants × M evaluators, with evaluator snapshots frozen at creation. See [Eval](/agent-testing/eval#eval-runs).

**Variant** — one column of an eval run: either **generate** (re-run a model over the datapoints) or **existing** (grade traces as-is). One variant is the **baseline**.

**Sample** — one datapoint's normalised transcript inside a run; can be marked **degraded** if it couldn't be faithfully reconstructed, excluding it from aggregates.

**Score** — one evaluator's result on one sample, with a typed outcome: `scored`, `abstained`, `not_applicable`, `skipped`, or `error`. Only `scored` results feed averages.

**Trace scoring** — evaluators running automatically on production traces as they're ingested, writing scores onto the trace. See [Observability](/core/observability#scores-on-arrival).

**Annotation** — a human label on a sample, used as ground truth for calibrating judges.

## Optimisation

**Optimisation run** — an experiment that edits an agent's code: candidate diffs are generated, replayed over a dataset, scored with a pinned eval set, and the winner ships as a PR. See [Optimisers](/agent-testing/optimisers).

**Iteration** — one loop cycle producing a batch of candidates. Iteration 0 is the **baseline** — the unmodified agent.

**Candidate** — one proposed code diff, scored by its own eval run against the best score so far.

**Executioner** — the local daemon (`overmind optimize`) that applies candidate diffs and runs your agent in your environment, reporting results back to the platform.

**Patience** — how many consecutive non-improving iterations a run tolerates before stopping early.

## Models

**Training run** — one or more parallel experiments over a training dataset, each producing a candidate model (API resource: `finetuning-jobs`). See [Training](/models/training).

**Baseline (training)** — the agent's production model, scored on the eval dataset at submit time; the reference every trained model's delta is measured against.

**Deployed model** — a served trained model with a copyable reference (`ft-...`) callable via the OpenAI-compatible API. Statuses: queued, quantizing, deploying, warming, ready, failed, deleting, deleted. See [Inference](/models/inference).

**Model PR** — a pull request Overmind opens that switches your agent's configured model to a trained model that beat the baseline.

**Credits** — the billing unit for compute Overmind performs (analyses, eval runs, training, serving). Trace ingest is never credit-gated.
