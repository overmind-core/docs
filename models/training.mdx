---
title: Training
description: Fine-tuning jobs — dataset validation, model recommendations, live loss curves, and the benchmark against your production model.
---

Training turns an agent's accumulated data into a model of your own. You pick a fine-tuning dataset, launch up to several experiments in parallel on managed GPU infrastructure, watch loss curves live, and every finished model is judged with your own evaluators against the model your agent runs today — per metric, before anything changes in production.

<Note>
Fine-tuning is in beta and not enabled for every account. If the Training page isn't visible in your console, contact [support@overmindlab.ai](mailto:support@overmindlab.ai) for access.
</Note>

## What a run needs

| Choice | Requirement |
| --- | --- |
| **Agent** | The agent whose job the model is learning; its production model becomes the benchmark baseline |
| **Training dataset** | A fine-tuning-intent dataset (chat-style messages). See [Datasets](/core/datasets#the-data-model) |
| **Eval dataset** | A separate eval-intent dataset the finished model is measured on |
| **Rubric** | The eval set that does the measuring, chosen from the agent's sets |

The train/eval separation is enforced, not advisory: an overlap check compares the two datasets' source traces so the benchmark never scores data the model saw in training (`GET /api/finetuning-jobs/dataset-overlap/`). Dataset validation runs before launch with the same checks the [Data Workshop](/core/datasets#the-data-workshop) applies at ingest, so problems surface as fixable findings up front instead of a failed job an hour in.

## Configuring experiments

The wizard recommends configurations from your dataset's statistics and cost/duration estimates, via the same endpoints you can call directly:

```bash
curl -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/models/"          # curated base-model catalog
curl -X POST -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/validate-dataset/"
curl -X POST -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/recommend/"
curl -X POST -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/estimate/"
```

A single run trains multiple experiments in parallel — each its own base model and settings — and the benchmark decides between them. Every recommended setting stays editable per experiment: epochs, learning rate, batch size, and context length (derived from your actual data), plus the training method — **adapter training (LoRA)**, with rank and alpha exposed, or **full fine-tuning**. The in-product model picker is the live source of truth for available base models; current families include Qwen and Llama instruct models.

## Watching a run

<Frame caption="A finished experiment: train loss per step, the learning-rate schedule, the run configuration (ctx 4096, batch 1, 1 epoch, lr 1e-4), and the deployed model reference.">
  <img src="/images/platform/training-run.png" alt="Training run monitor with loss curve and config" />
</Frame>

The monitor streams per-step metrics for every experiment — train loss, learning-rate schedule, token accuracy (train and validation), and gradient norm — with progress, ETA, tokens processed, and credits used. The same data is available raw:

```bash
curl -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/{id}/loss-curves/"
curl -H "X-Api-Key: $OVERMIND_API_KEY" "https://api.overmindlab.ai/api/finetuning-jobs/{id}/events/"
```

A run going wrong is visible in the curves long before it ends — validation loss climbing away from train loss — and any experiment can be cancelled individually and retried later (`POST .../cancel/`, `POST .../retry/`).

<Frame caption="Two parallel experiments: one cancelled at 75%, one succeeded with a +21% score against the baseline and its model auto-deployed.">
  <img src="/images/platform/finished-job.png" alt="Parallel experiments with cancel and success states" />
</Frame>

## The benchmark: your fine-tune vs. your production model

Each job spawns judge evaluations at two points, both scored on your chosen eval dataset with your chosen rubric:

1. **Baseline** — the agent's production model, snapshotted at submit time.
2. **Final** — the finished fine-tune.

The headline number is the delta per metric. The comparison is deliberately against the model your agent actually runs — not the untouched base model — because that's the comparison a switch decision needs.

<Frame caption="The Evals table under the metrics: Baseline and Final rows per experiment, scored with the same rubric.">
  <img src="/images/platform/training-metrics.png" alt="Training metrics with baseline and final eval scores" />
</Frame>

## After training

A succeeded experiment deploys its model automatically — no separate publish step. It appears in [Inference](/models/inference) with a copyable model reference (shaped like `ft-<id>-<base>`), on the agent's **Models** tab, and its weights are downloadable. Everything stays linked: a served model traces back to its training job, dataset, and the eval scores that justified it.

When the benchmark says the fine-tune wins, **Model PR** (`POST /api/finetuning-jobs/{id}/create-model-pr/`) edits your repository to point the agent at the new model and opens the pull request — the switch happens through your normal review, never automatically.

## Good to know

- Model quality comes almost entirely from the data. Get the Workshop verdict to **Ready** before spending on a run; a few hundred clean, trace-sourced rows beat thousands of noisy ones.
- The training dataset is [locked](/core/datasets#the-data-model) while the job runs. Finish curation first, or duplicate the dataset.
- Each experiment reports its final credit cost; estimates are shown before you commit.
